#ifndef _FOUNDATION_BASEDEFINE_H
#define _FOUNDATION_BASEDEFINE_H

#define FUZZ 1E-7
#define ABS(x) ((x > 0) ? (x) : -(x))
#define tMIN(x,y) (((x) < (y)) ? (x) : (y))
#define tMAX(x,y) (((x) > (y)) ? (x) : (y))

#endif

